<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dinner.dao.beforeUserLoginDao.BeforeUserLoginDao">
    <select id="checkUser" resultType="map" >
      select * from  tab_vip where vip_phone = #{userid} and vip_password = #{passwordinput}
    </select>
</mapper>