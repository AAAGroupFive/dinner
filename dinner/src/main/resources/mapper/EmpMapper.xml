<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dinner.dao.emp.EmpDao">
    <select id="getEmp" resultType="map">
        select * from(
          select rownum rn,EMP_ID as empId,EMP_NAME as EmpName,EMP_USER as userName,EMP_PHONE as phone,EMP_PASS as password,emp_birth as birth,EMP_ADDR as addr,EMP_ROLE as role from tab_emp
        <where>
            and rownum &lt; #{end}
        </where>
        ) a where a.rn &gt; #{start}
    </select>
    <select id="getCountEmp" resultType="int">
        select count(*) from tab_emp
    </select>
    <insert id="addEmp" parameterType="map">
        <selectKey keyProperty="empNo" order="BEFORE" resultType="int">
            select seq_emp_id.nextval from dual
        </selectKey>
        insert into tab_emp values(#{empNo},#{EmpName},#{userName},#{password},#{sex},#{addr},#{role},to_date(#{birth},'yyyy-mm-dd'),#{phone})
    </insert>
</mapper>
